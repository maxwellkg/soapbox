<%= form_with url: admin_subscribers_path, method: :get do |form| %>
  <fieldset class="admin-search-fieldset">
    <legend>search and filter</legend>

    <div class="admin-search-row">
      <div class="admin-search-input">
        <%= form.label :search, "search subscribers", class: "invisible" %>
        <%= form.search_field :search, value: search_term, placeholder: "search for a subscriber" %>
      </div>

      <div class="admin-search-select">
        <%= form.label :status, "status", class: "invisible" %>
        <%= form.select :status,
                        options_for_select(admin_subscribers_status_options, admin_subscribers_selected_status),
                        { include_blank: "all statuses" } %>
      </div>

      <div class="admin-search-actions">
        <%= form.button "search", class: "btn btn-primary" %>
      </div>
    </div>
  </fieldset>
<% end %>
