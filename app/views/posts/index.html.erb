<% content_for :title, @blog.title %>

<% if authenticated? %>
  <section class="page-actions">
    <%= link_to "go to admin", admin_root_path, class: "btn" %>
  </section>
<% end %>

<header>
  <%= render "blog_title" %>
</header>

<% if @blog.description? %>
  <section id="description">
    <%= @blog.description %>
  </section>
<% end %>

<section class="signup-section">
  <%= render "subscribers/signups/form" %>
</section>

<section class="posts-search-section">
  <%= render "search_form" %>
</section>

<section id="posts">
  <% if search_term.present? %>
    <p><%= pluralize(@page.unpaginated_record_count, "matching post") %></p>
  <% end %>

  <% if @posts.any? %>
    <%= render partial: "posts/preview", collection: @posts, as: :post %>
    <%= render_pagination_controls %>
  <% else %>
    <p><%= no_matching_posts_text %></p>
  <% end %>
</section>
